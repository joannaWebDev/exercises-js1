/** Complete the function in exercise.js so that it halves the input  OK

> Remember to use the return keyword to get a value out of the function

## Expected result

```
6*/
var result = halve(12);

  function halve(number) {
    return number/2;
  }

console.log(result);

/* Try calling the function more than once with some different numbers*/

  result ++;
  console.log(result);
  
